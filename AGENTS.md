# AGENTS.md

## 專案背景

使用者是財金系大三學生，課程作業需要完成一個和財金相關的網頁或程式設計專案。專案不應只是展示頁面，而是要能真正使用、簡單易懂、操作便利，並且可以發展成實用的財金領域工具。

目前專案方向為「定期定額投資試算網站」，目標使用者包含大學生、小資族與投資新手。系統應協助使用者理解定期定額、複利、投資成本、通膨與目標金額之間的關係。

## 開發目標

- 建立可真正使用的財金工具，而不是只有靜態介紹頁。
- 介面要清楚、直覺、便利，適合非技術背景使用者。
- 功能要能支援課堂報告、現場展示與實際操作。
- 系統結果要有清楚解釋，避免使用者誤解成保證獲利。
- 專案需能持續更新、版本控管、部署與交付。

## 技術方向

優先使用目前專案既有技術：

- React
- Vite
- Tailwind CSS
- Recharts
- JavaScript

除非有明確必要，不要任意改用新的大型框架或重寫架構。新增功能應盡量沿用目前的元件結構、樣式風格與資料計算方式。

## 財金工具設計原則

- 所有輸入欄位都要有明確中文標籤。
- 試算結果要提供中文說明，讓投資新手能理解數字意義。
- 圖表需使用中文標題、中文提示與金額格式。
- 財金結果必須保留假設條件與限制說明。
- 不得使用「保證獲利」、「穩賺」、「一定達標」等不當字眼。
- 應明確區分「情境試算」與「投資建議」。
- 免責聲明必須保留：

```text
本系統僅供財金資訊系統開發課程報告與情境試算使用，不構成任何投資建議。實際投資仍須考量個人風險承受度、市場波動、稅費與金融商品特性。
```

## 使用者體驗要求

- 首頁應直接呈現可操作的工具，不要做成只有文案的 landing page。
- 電腦與手機都要能正常使用。
- 表單、結果卡片、圖表、情境比較與說明區塊都要清楚分區。
- 金額、百分比與年期要格式化，避免使用者讀錯。
- 操作流程應盡量減少步驟，輸入後即時更新結果。
- 匯出功能應產生可讀的文字報告，方便附在作業或展示資料中。

## 圖片與視覺素材規則

- 若需要圖片內容，可使用 image2 生成。
- 生成圖片應符合財金、投資、資料視覺化、學生專題或工具介面情境。
- 圖片不得暗示保證獲利、奢華致富或高風險投機。
- 若圖片用於首頁或報告，應輔助理解工具用途，不應喧賓奪主。
- 優先使用乾淨、可信、現代化的財金工具視覺風格。

## 儲存庫與版本控管

此專案應建立 Git 儲存庫，用來保存現有成果並依進度持續更新。

建議流程：

1. 以 `dca-investment-calculator-final-report` 作為儲存庫根目錄。
2. 初始化 Git：

```bash
git init
```

3. 建立 `.gitignore`，至少排除：

```text
node_modules/
dist/
.env
.env.*
```

4. 每次完成一個明確進度後再 commit。
5. commit 訊息要簡短清楚，例如：

```text
feat: add investment calculator dashboard
docs: add final report documents
fix: improve target monthly investment calculation
```

6. 若要上傳 GitHub，避免提交個人帳密、API key、`.env` 或不必要的大型檔案。

## 部署要求

專案應可部署到靜態網站平台，例如：

- Vercel
- Netlify
- GitHub Pages

部署前必須確認：

```bash
npm install
npm run build
```

`npm run build` 必須成功，才視為可部署版本。

若使用 Vercel 或 Netlify：

- Build command：`npm run build`
- Output directory：`dist`
- Framework preset：Vite 或 React

## 資安要求

此專案目前主要是前端試算工具，但仍需注意基本資安。

必須遵守：

- 不要在前端放入任何 API key、帳號密碼或個人憑證。
- 不要提交 `.env`、`.env.local`、憑證檔或私密設定。
- 使用者輸入只做數值計算，不應直接插入 HTML。
- 匯出文字檔內容應來自受控欄位，避免執行任何外部腳本。
- 所有套件安裝後應注意 `npm audit` 結果。
- 部署前確認沒有多餘測試檔、個人路徑或敏感資料外洩。
- 若未來加入後端、資料庫或登入功能，必須重新評估驗證、授權、資料加密與輸入驗證。

## 品質檢查

每次完成修改後，至少執行：

```bash
npm run build
```

若修改與畫面有關，應實際開啟網站確認：

```bash
npm run dev
```

檢查項目：

- 首頁是否正常載入。
- 手機版是否不破版。
- 表單輸入是否會即時更新。
- 圖表是否正常顯示。
- 匯出 txt 是否可下載且內容完整。
- 免責聲明是否仍存在。

## 文件維護

若功能有變更，需同步更新：

- `README.md`
- `操作說明書.md`
- `期末報告簡報.md`
- `demo-data.txt`，若示範案例有變動

文件內容應使用老師與同學看得懂的語氣，不要只寫工程術語。

## 後續可延伸方向

可以依課程進度逐步新增：

- 多組投資方案比較
- ETF / 基金 / 定存情境模板
- 配息再投入試算
- 稅費與手續費細項
- 歷史資料回測
- Excel 或 PDF 匯出
- GitHub Pages / Vercel 部署
- 更完整的報告圖表與簡報素材

## 工作原則

- 優先完成可用功能，再做視覺美化。
- 不做過度複雜、超出作業需求太多的架構。
- 每次修改都要考慮是否會影響 build、部署與老師展示。
- 財金結論要保守、清楚、有免責聲明。
- 專案成果應能被學生本人理解、操作與說明。

